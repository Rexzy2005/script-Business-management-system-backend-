# Business Backend

Project for business management backend API.

## Setup

See `.env.example` for environment variables.

## Scripts

- `npm start` — Start server
- `npm run dev` — Start server with nodemon
- `npm test` — Run tests
